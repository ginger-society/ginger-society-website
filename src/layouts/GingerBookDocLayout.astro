---
import DocLayout from "./DocLayout.astro";
import { menuItems } from "../contents/ginger-book-side-menu-config";

const { title } = Astro.props;
---

<DocLayout
  title={`${title} | GingerBook`}
  menuItems={menuItems}
  logo="/img/ginger-book.png"
  menuTitle="Ginger Book User guide"
>
  <slot />
</DocLayout>
